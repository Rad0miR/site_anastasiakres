import V1App from './v1/V1App.jsx'

/**
 * Корень сайта — сюда переедет новая версия.
 *
 * Пока она не написана, «/» показывает ту же первую версию, что и «/v1»:
 * рабочий адрес не должен ломаться на время переработки. Когда новый
 * дизайн будет готов, содержимое этого файла заменяется целиком —
 * папку src/v1 при этом трогать не нужно, она открывается по своему адресу.
 */
export default function App() {
  return <V1App />
}
