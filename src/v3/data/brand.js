/**
 * Логотип. Единственная строка сайта, которая не переводится: «Kres AI» —
 * имя собственное и на всех трёх языках выглядит одинаково.
 *
 * Делится на две части только чтобы покрасить «AI» в розовый; для
 * копирования и для скринридера это одно слово (см. components/Wordmark).
 */
export const brand = { first: 'Kres', accent: 'AI' }
