import en from './en'
import uk from './uk'
import de from './de'

/**
 * Весь текст сайта Kres AI — три файла, по одному на язык.
 *
 * Дерево у всех трёх одинаковое (см. en.js — он исходный), поэтому
 * поменять формулировку значит открыть один файл и поправить одну строку.
 * Вёрстку трогать не нужно.
 *
 * Названия площадок, ссылки, иконки и картинки лежат отдельно —
 * social.js, contentTypes.js, styles.js: там к каждой записи привязано
 * что-то, кроме текста. Подписи к ним живут здесь, по тому же id.
 *
 * ЧТОБЫ ДОБАВИТЬ ЯЗЫК: положить рядом файл по образцу en.js и дописать
 * его сюда — в dictionaries и в LANGUAGES. Больше нигде ничего менять
 * не нужно: и кнопка в шапке, и список берут языки отсюда.
 */
export const dictionaries = { uk, en, de }

/**
 * Языки в том порядке, в котором они стоят в списке под кнопкой.
 *
 *   id   — он же ключ в dictionaries и значение <html lang>
 *   name — название на самом языке, а не в переводе: так принято
 *          в переключателях, и так его узнает тот, кто ищет свой
 *   flag — ключ из flagIcons (components/icons.jsx)
 */
export const LANGUAGES = [
  { id: 'uk', name: 'Українська', flag: 'ua' },
  { id: 'en', name: 'English', flag: 'gb' },
  { id: 'de', name: 'Deutsch', flag: 'de' },
]

/** С какого языка сайт открывается впервые. */
export const DEFAULT_LANGUAGE = 'en'
